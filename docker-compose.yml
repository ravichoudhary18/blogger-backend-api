services:
  # db:
  #   image: postgres:17-alpine
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data/
  #   env_file: .env

  web:
    build: .
    volumes:
      - .:/app
      - ./mount:/mnt
    ports:
      - "8000:8000"
      - "8015:8015"
    env_file: .env
    # depends_on:
    #   - db

# volumes:
#   postgres_data:
